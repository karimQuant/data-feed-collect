# Example .env file for data-feed-collect

# ClickHouse Database Connection Details
# These variables are used by the DataBase class if not provided explicitly.
# They are kept here for reference or other potential uses,
# but SQLAlchemy connection will primarily use DATABASE_URL.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=market_data
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# SQLAlchemy Database Connection URL
# This is the primary variable used for SQLAlchemy connections.
# Examples:
# PostgreSQL: postgresql+psycopg2://user:password@host:port/dbname
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/market_data
