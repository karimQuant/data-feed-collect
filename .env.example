# Example .env file for data-feed-collect

# ClickHouse Database Connection Details
# These variables are used by the DataBase class if not provided explicitly.
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=9000 # Or the NodePort if connecting from outside the cluster (e.g., 30000)
CLICKHOUSE_DATABASE=default
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
